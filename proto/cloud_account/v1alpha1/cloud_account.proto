// Copyright 2026 Cofide Limited.
// SPDX-License-Identifier: Apache-2.0

// TODO - update
// This file defines the ExchangePolicy message and related types used to
// control Credex token exchanges within a trust zone. Exchange policies specify
// match conditions on inbound tokens and determine whether the exchange is
// permitted or denied.

syntax = "proto3";

package proto.cloud_account.v1alpha1;

import "google/api/field_behavior.proto";
import "google/protobuf/timestamp.proto";
import "proto/cloud_account/v1alpha1/aws_cloud_account.proto";

option go_package = "github.com/cofide/cofide-api-sdk/gen/go/proto/cloud_account/v1alpha1";

// CloudAccount represents a cloud provider account linked to a Cofide organization.
message CloudAccount {
  // Unique identifier for the cloud account. Generated by Connect.
  string id = 1 [(google.api.field_behavior) = IDENTIFIER];
  // Cofide organization ID this cloud account belongs to.
  string org_id = 2 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.field_behavior) = IMMUTABLE
  ];
  // Human-readable name for the cloud account within Connect.
  string name = 4 [(google.api.field_behavior) = REQUIRED];
  // Cloud provider-specific configuration.
  oneof provider {
    AWSAccount aws = 5;
  }
  // Time of resource creation.
  google.protobuf.Timestamp created_at = 6 [(google.api.field_behavior) = OUTPUT_ONLY];
  // Time of last resource update.
  google.protobuf.Timestamp last_updated_at = 7 [(google.api.field_behavior) = OUTPUT_ONLY];
}
