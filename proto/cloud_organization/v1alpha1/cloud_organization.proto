// Copyright 2026 Cofide Limited.
// SPDX-License-Identifier: Apache-2.0

// This file defines the CloudOrganization message and related types used to
// represent a cloud provider organization linked to a Cofide organization.

syntax = "proto3";

package proto.cloud_organization.v1alpha1;

import "google/api/field_behavior.proto";
import "google/protobuf/duration.proto";
import "google/protobuf/timestamp.proto";
import "proto/cloud_provider/v1alpha1/cloud_provider.proto";

option go_package = "github.com/cofide/cofide-api-sdk/gen/go/proto/cloud_organization/v1alpha1";

// AWSOrganization contains AWS-specific configuration for a cloud organization.
message AWSOrganization {
  // AWS Organization ID (e.g., o-xxxxxxxxxx).
  string aws_org_id = 1 [(google.api.field_behavior) = REQUIRED];
  // Audience value for the initial SPIFFE JWT-based assume role call.
  // Only used when assume_through_oidc is true.
  string audience = 2 [(google.api.field_behavior) = REQUIRED];
  // Ordered chain of IAM roles to assume. When assume_through_oidc is true, the
  // first role is assumed via AssumeRoleWithWebIdentity using the SPIFFE JWT;
  // when false, the first role is assumed via plain AssumeRole using ambient
  // credentials (e.g. EKS Pod Identity). Each subsequent role is always assumed
  // using the credentials from the prior step.
  repeated proto.cloud_provider.v1alpha1.AWSAssumeRoleConfig role_chain = 3 [(google.api.field_behavior) = REQUIRED];
  // Whether the first role in role_chain is assumed via SPIFFE JWT-based
  // AssumeRoleWithWebIdentity (true, the default) or via ambient credentials
  // such as EKS Pod Identity (false).
  bool assume_through_oidc = 4 [(google.api.field_behavior) = OPTIONAL];
}

// CloudOrganization represents a cloud provider organization linked to a Cofide organization.
message CloudOrganization {
  // Unique identifier for the cloud organization. Generated by the server.
  string id = 1 [(google.api.field_behavior) = IDENTIFIER];
  // Cofide organization ID this cloud organization belongs to.
  string org_id = 2 [
    (google.api.field_behavior) = REQUIRED,
    (google.api.field_behavior) = IMMUTABLE
  ];
  // Human-readable name for the cloud organization.
  string name = 3 [(google.api.field_behavior) = REQUIRED];
  // Cloud provider-specific configuration.
  oneof provider {
    AWSOrganization aws = 4;
  }
  // Whether discovery is enabled for this cloud organization.
  bool discovery_enabled = 5 [(google.api.field_behavior) = OPTIONAL];
  // Current discovery status.
  proto.cloud_provider.v1alpha1.DiscoveryStatus status = 6 [(google.api.field_behavior) = OUTPUT_ONLY];
  // Timestamp of the last successful discovery.
  google.protobuf.Timestamp last_discovered_at = 7 [(google.api.field_behavior) = OUTPUT_ONLY];
  // Time of resource creation.
  google.protobuf.Timestamp created_at = 8 [(google.api.field_behavior) = OUTPUT_ONLY];
  // Time of last resource update.
  google.protobuf.Timestamp last_updated_at = 9 [(google.api.field_behavior) = OUTPUT_ONLY];
  // How frequently discovery runs for this cloud organization (e.g. every minute).
  google.protobuf.Duration discovery_interval = 10 [(google.api.field_behavior) = OPTIONAL];
}
